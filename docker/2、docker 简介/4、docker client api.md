## Docker client api

Docker 提供了用于与Docker 守护进程交互的API，称为DockerEngine API以及 还提供了 python 与 go两种sdk，如果 sdk 不适用的话，则可以直接使用 DockerEngine API

Docker Engine API 是一种Restful API， 可以通过 HTTP 客户端（例如 wget 、curl） 或者其他的HTTP库 进行访问

具体教程 请异步 Docker engine 官方文档查看  https://docs.docker.com/engine/api/sdk/

