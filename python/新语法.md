### pass

以前可以直接用pass 表示没有任何逻辑，现在可以用...代替

```
def demo1():
	pass
	
def demo2():
	...
```



### 箭头函数

```
def extend(self, iterable: Iterable[_T]) -> None: ...
def reverse(self) -> None: ...
def pop(self, index: int = ...) -> _T: ...
```

